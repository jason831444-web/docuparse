# Archived Evaluation Reports

Historical timestamped reports live here so the active reports directory stays readable.

Current comparison handles remain in `backend/eval/reports/latest*.json` and `backend/eval/reports/latest*.md`.
